# PRD: 감사 전 재무비율 분석 도구 (Pre-Audit Financial Ratio Analyzer)

## 1. 개요

| 항목 | 내용 |
|---|---|
| **What** | DART 전자공시 감사보고서 링크를 입력받아 재무제표를 자동 추출하고, 전전기·전기·당기 3개년의 유동비율/부채비율/자기자본비율/ROA/ROE/PER/PBR 등을 테이블과 그래프로 제시하는 프로그램 |
| **Why** | 본격적인 감사절차 착수 전, 중요왜곡표시위험(RMM)이 발생할 수 있는 비율을 사전에 포착하고, 당기 감사의견 형성의 근거자료로 활용하기 위함 |
| **Who / Where** | 회계법인 소속 회계사 (감사팀) |
| **When** | 본격적인 감사절차 시작 전 (예비조사/위험평가 단계) |
| **How** | 감사보고서 링크 → 재무제표 파싱 → 비율 계산 → 테이블 제시 → 그래프 시각화 |

## 2. 목표 및 성공 기준

- 감사팀이 DART 링크 하나만 입력하면 5분 내에 3개년 재무비율 분석표를 확보할 수 있어야 함
- 계산된 비율이 실제 재무제표 수치와 교차검증했을 때 오차 없이 일치해야 함
- 전기 대비 이상 변동(예: 부채비율 급증, 유동비율 급락)이 시각적으로 즉시 식별 가능해야 함

## 3. 사용자 스토리

- 회계사로서, 나는 DART 감사보고서 링크를 붙여넣기만 하면 회사명과 사업연도를 자동으로 인식하고 싶다.
- 회계사로서, 나는 전전기/전기/당기 비율을 한 테이블에서 비교하고 싶다.
- 회계사로서, 나는 비율의 3개년 추이를 그래프로 보고 이상치를 빠르게 파악하고 싶다.
- 회계사로서, 나는 계정과목 매핑이 실패한 경우 경고를 받아서 수치를 맹신하지 않고 싶다.
- 회계사로서, 나는 비율이 3개년 내내 안정적으로 보여도 유동자산/비유동자산 등 절대금액 구성이 크게 바뀌었는지 원 항목 그래프로 확인하고 싶다. (비율이 동일해도 절대 규모가 함께 축소·확대되거나 자산·부채 구성비가 이동하는 경우, 비율만으로는 이 변화가 드러나지 않기 때문)

## 4. 범위 (Scope)

### 포함
- DART 감사보고서(사업보고서 첨부 감사보고서) 뷰어 링크 파싱
- 단일회사 3개년 재무비율 계산 및 표/그래프 제시
- 계정과목 매핑 실패 시 경고 표시

### 제외 (1차 버전 기준)
- 여러 회사 동시 비교 (추후 확장)
- 연결/별도 재무제표 동시 비교 (1차는 하나만 선택)
- 감사의견 자동 판단 로직 (수치 제시까지만, 판단은 회계사 몫)

## 5. 단계별 구현 로드맵

### Phase 1 — 링크 파싱 및 API 연동
**목표**: DART 감사보고서 링크에서 필요한 식별자를 추출하고, 재무제표 원본 데이터를 가져온다.

- DART 뷰어 URL(예: `dart.fss.or.kr/dsaf001/main.do?rcpNo=...`)에서 `rcept_no`(접수번호) 파싱
- `rcept_no`로 공시서류 원문 조회 API 또는 고유번호 API를 통해 `corp_code`, `bsns_year`, `reprt_code` 역산
- `fnlttSinglAcntAll`(단일회사 전체 재무제표) API 호출 → 당기/전기/전전기 원본 계정 데이터 확보
- **Definition of Done**: 링크 하나를 입력하면 회사명, 사업연도, 3개년 원본 재무제표 데이터프레임이 출력됨

**주의사항**
- 감사보고서 자체는 별도 첨부문서(XML/PDF)로 존재하고, 재무수치는 사업보고서 재무제표 API에서 가져오는 구조이므로 "감사보고서 링크 → 사업보고서 재무제표"로 연결하는 매핑 로직이 필요함
- rcept_no만으로 corp_code를 바로 못 얻는 경우, 공시서류 원문 API(document.xml)를 통한 역조회 단계가 하나 더 필요할 수 있음 → 이 부분은 실제 링크로 먼저 테스트 필요

### Phase 2 — 재무비율 계산 및 테이블 제시
**목표**: 원본 계정 데이터를 표준화하고 핵심 재무비율을 3개년치 계산한다.

계산 대상 비율:
- 유동비율 = 유동자산 / 유동부채
- 부채비율 = 부채총계 / 자본총계
- 자기자본비율 = 자본총계 / 자산총계
- ROA = 당기순이익 / 자산총계(평균)
- ROE = 당기순이익 / 자본총계(평균)
- 매출총이익률, 영업이익률, 순이익률 (감사 위험평가에 유용하여 추가 권장)

- 계정과목명 표준화 딕셔너리 구축 (회사별 상이한 명칭 대응: "매출액"/"영업수익" 등)
- 매핑 실패 계정은 화면에 경고로 표시, 수치는 공란 처리 (임의 추정 금지)
- 결과를 "연도 / 비율명 / 값" 롱포맷 데이터프레임으로 반환 → 추후 확장 대비
- 표준화된 계정(유동자산·유동부채·비유동자산·비유동부채 등)의 원 금액(절대값)도 비율과 별도로 보존 → Phase 3에서 "원 항목 그래프(절대금액 추이)" 제공을 위한 선행 데이터로 사용
- 테이블 UI: 전전기·전기·당기를 컬럼으로, 비율명을 행으로 배치, 전기 대비 증감률 컬럼 추가
- **Definition of Done**: 회사 하나에 대해 3개년 비율 표가 정확히 출력되고 CSV 다운로드 가능

### Phase 3 — 그래프 시각화
**목표**: 비율별 3개년 추이를 시각적으로 비교하고 이상 변동을 강조한다. 다만 비율만으로는 절대 규모/구성 변화가 드러나지 않으므로, 비율 그래프를 보완하는 원 항목(절대금액) 그래프를 함께 제공한다.

**왜 원 항목 그래프가 필요한가** (감사 관점 근거)
- 비율이 3개년 내내 동일해도(예: 유동비율 150% 유지) 유동자산·유동부채가 둘 다 절반으로 줄어드는 등 절대 규모 축소는 비율에 드러나지 않음 → 사업 축소·매출 급감 신호를 놓칠 수 있음
- 유동/비유동 자산·부채의 구성비가 전기 대비 크게 이동한 경우(예: 비유동자산 급증) 대규모 설비투자, 자산 재평가, 분류 오류 가능성을 시사하나 이는 부채비율·유동비율 같은 비율 지표에는 직접 드러나지 않음
- 중요왜곡표시위험(RMM)/중요성(materiality) 판단은 궁극적으로 금액 기준이므로, 비율만으로는 특정 계정이 재무제표 전체에서 차지하는 비중을 알기 어려움

**구현 항목**
- 비율 그래프 (메인, 1순위): 유동비율/부채비율/자기자본비율/ROA/ROE 등 라인차트/바차트 (plotly)
- 전기 대비 임계치(예: ±20% 이상 변동) 초과 시 강조 표시 (색상 변경 또는 주석)
- 유동성/레버리지/수익성 지표를 탭 또는 섹션으로 그룹화하여 배치
- 원 항목 그래프 (보조/드릴다운, 2순위): 유동자산·유동부채·비유동자산·비유동부채 등을 3개년 스택 바 차트로 제시 — "왜 이 비율이 나왔는지"를 설명하는 근거자료 역할
- 화면 배치: 비율 그래프를 기본 화면에 우선 노출하고, 원 항목 그래프는 그 아래 섹션 또는 별도 탭("상세보기 — 원 항목 추이")에 배치하여 정보 과부하 없이 이상 신호 발견 시에만 드릴다운 가능하게 구성 (요약 지표로 스크리닝 → 이상 신호 발견 시 원 항목으로 원인 파고들기라는 실제 감사 위험평가 워크플로우에 대응)
- **Definition of Done**: 테이블에서 확인한 이상 변동이 비율 그래프에서 한눈에 식별 가능하고, 비율이 안정적이어도 절대금액 구조가 크게 바뀐 경우는 원 항목 그래프에서 포착 가능

### Phase 4 — PER/PBR 확장 (별도 데이터 소스 필요)
**목표**: 시장가치 기반 비율을 추가한다.

- ⚠️ DART API는 재무제표만 제공하고 주가 데이터는 제공하지 않음 → 별도 소스 필요
- 상장사 주가/시가총액: 네이버금융, KRX 정보데이터시스템, 또는 유료 시세 API 중 선택
- EPS = 당기순이익 / 발행주식수, BPS = 자본총계 / 발행주식수 (발행주식수는 DART 재무제표 주석 또는 별도 API에서 확보)
- PER = 주가 / EPS, PBR = 주가 / BPS
- 비상장사의 경우 PER/PBR 산출 불가 → UI에서 "비상장사는 해당 비율 계산 불가" 안내 필요
- **Definition of Done**: 상장사 기준으로 PER/PBR이 정확히 계산되고, 비상장사는 명확한 안내 문구가 표시됨

### Phase 5 — 배포 및 보안 처리
**목표**: GitHub/Supabase 배포 시 API 키 노출 및 서비스 안정성 문제를 방지한다.

- API 키는 `.env` + `.gitignore` 처리, 배포 플랫폼의 시크릿 매니저 사용
- 사용자가 직접 자신의 DART API 키를 입력하는 방식 유지 (서버 키 노출 최소화)
- 과도한 호출 방지를 위한 캐싱 및 요청 제한 로직
- 면책 문구 추가: "본 도구는 참고용 분석 자료이며 감사의견 형성의 유일한 근거가 될 수 없음"

## 6. 비기능 요구사항

- **정확성**: 계산된 수치는 반드시 원본 공시자료와 대사(reconciliation) 가능해야 함
- **투명성**: 계정 매핑 로직이 임의 추정을 하지 않고, 실패 시 명확히 표시
- **감사 실무 적합성**: 단순 투자지표가 아니라 위험평가 관점에서 유의미한 변동을 강조하는 방향으로 설계

## 7. 리스크 및 오픈 이슈

| 리스크 | 대응 방안 |
|---|---|
| 감사보고서 링크 → corp_code 역산이 API만으로 부족할 수 있음 | Phase 1에서 실제 링크로 프로토타입 테스트 후 로직 확정 |
| 계정과목명 회사별 상이 | 표준 매핑 딕셔너리 + 실패 시 수동 확인 경고 |
| PER/PBR용 주가 데이터 소스 부재 | Phase 4에서 별도 소스 선정 (법적/이용약관 확인 필요) |
| API 이용횟수 제한 | 캐싱, 재시도 로직, 사용자별 키 입력 방식 유지 |
| 계산 오류가 감사판단에 잘못 활용될 가능성 | 면책 문구 명시, 최종 판단은 회계사 책임임을 UI에 고지 |

## 8. 다음 액션

Phase 1부터 순서대로 클로드코드에 구현 요청. 각 Phase 완료 시 실제 DART 링크로 검증 후 다음 단계 진행 권장.

## 9. 현재 진행 상황 (Progress Log)

### Phase 1 — 진행 중
- Python 스택으로 결정 (requests + pandas, 추후 Streamlit UI로 확장 예정)
- `.env`에 `DART_API_KEY` 저장 완료, `.gitignore` 처리 완료
- 구현 완료 모듈:
  - `src/link_parser.py` — DART 뷰어 URL에서 rcept_no(14자리) 추출
  - `src/dart_client.py` — corpCode.xml 캐싱, list.json 공시검색, fnlttSinglAcntAll.json 재무제표 조회
  - `src/resolver.py` — rcept_no → corp_code/corp_name/bsns_year 역산 (rcept_no 앞 8자리=접수일자 규약 이용, list.json으로 매칭)
  - `src/financials.py` — 원본 응답 → 롱포맷/와이드 테이블 변환
  - `main.py` — CLI 진입점
  - `tests/` — link_parser, resolver(mock), financials 단위 테스트
- **미검증 항목 (실제 DART 링크로 검증 필요)**:
  - resolver의 rcept_no → corp_code 역산 로직 (list.json 페이징이 실제로 해당일 전체 공시를 커버하는지)
  - report_nm에서 사업연도 추출 정규식이 실제 감사보고서 서류명과 맞는지
  - 사용자가 테스트 링크를 아직 제공하지 않아 실제 API 응답으로는 미검증 상태

### 2026-07-16 — 의존성 설치 및 오프라인 테스트/에러 핸들링 점검
- `.venv`에 requirements.txt 의존성 설치 완료 (requests, pandas 3.0.3, python-dotenv, pytest 9.1.1 등)
- `pytest tests/` 15건 전체 통과 (네트워크 호출 없이 오프라인, 0.67s)
  - 최초 실행 시 `test_financials.py::test_to_long_format_parses_amounts_and_marks_failures_as_none` 1건 실패
    - 원인: `to_long_format`에서 파싱 실패 계정을 `amount=None`으로 넣지만, DataFrame 생성 시 amount 컬럼이 float dtype으로 캐스팅되며 `None`이 `NaN`으로 변환됨. 테스트가 `is None`으로 단정해 실패.
    - 프로덕션 코드는 정상(오히려 NaN이 비율 계산 시 예외 없이 전파되어 "공란 처리" 원칙에 더 부합) → `tests/test_financials.py`의 단정문을 `pd.isna(...)`로 수정하여 해결. 계산 로직/공란 처리 원칙 자체는 변경 없음.
- `main.py` 에러 핸들링 경로 검증 및 버그 수정:
  - `python main.py "not-a-valid-url"` → `InvalidDartLinkError` 정상 캐치, 스택트레이스 없이 exit code 1로 종료 확인.
  - 버그 발견: `resolve_disclosure`/`fetch_financial_statements` 호출 중 발생하는 `DartApiError`(및 API 키 미설정 시 `RuntimeError`)가 `main.py`에서 캐치되지 않아 원본 스택트레이스와 내부 파일 경로가 그대로 사용자에게 노출됨. `main.py`에 `API_ERRORS` 캐치 블록을 추가해 사용자 친화적 메시지로 exit code 1 종료하도록 수정.
  - 버그 발견: Windows 콘솔에서 활성 코드페이지가 UTF-8(65001)이어도 Python이 stdout/stderr 인코딩을 cp949로 잡아 한글 에러 메시지가 깨져 표시됨(PowerShell/Git Bash 모두 재현). `main()` 시작부에 `sys.stdout`/`sys.stderr`를 `reconfigure(encoding="utf-8")`로 재설정하여 해결, 한글 정상 출력 확인.
  - 실제 DART_API_KEY로 실제 rcept_no를 조회하는 end-to-end 테스트는 미실시(사용자가 실제 링크 미제공, 요청 범위 외).

### 2026-07-16 — Phase 1 실제 DART 링크 검증 완료
- 사용자 제공 링크(`dart.fss.or.kr/dsaf001/main.do?rcpNo=20260310002820&dcmNo=11104487`, 삼성전자 2025 사업보고서)로 `main.py` 실행
- 결과: rcept_no→corp_code 역산(00126380, 삼성전자), 사업연도 추정(2025) 모두 정확히 동작
- 3개년(전전기/전기/당기) 재무제표 87개 계정 전체 파싱 성공, 계정과목 매핑 실패([경고]) 0건
- **결론**: Phase 1의 미검증 항목(resolver 역산 로직, report_nm 사업연도 정규식)이 실제 데이터로 검증 완료됨 → Phase 2(재무비율 계산) 착수 가능

### 2026-07-16 — Phase 2 재무비율 계산 구현 및 실데이터 검증 완료
- 구현 완료 모듈:
  - `src/ratios.py` — 계정과목 표준화(`standardize_accounts`) + 8개 재무비율 계산(`compute_ratios`)
  - 계정과목 표준화는 `account_id`(IFRS 표준계정코드) 완전일치를 1순위, `account_nm`(회사 기재 계정과목명) 완전일치를 폴백으로 사용. 회사마다 계정명이 달라도(예: "매출액"/"영업수익") 표준코드가 같으면 정확히 매칭됨. 두 방식 모두 실패하면 공란+경고 처리(임의 추정 금지).
  - `src/financials.py`의 `to_long_format`에 `account_id` 컬럼 추가 (기존 `account_nm`만으로는 표준화 신뢰도가 낮아 필요했음)
  - 계산 비율: 유동비율, 부채비율, 자기자본비율, ROA, ROE, 매출총이익률, 영업이익률, 순이익률 (전기대비증감률 컬럼 포함)
  - 매출총이익을 별도 공시하지 않는 회사가 많아, 매출액/매출원가가 모두 확보된 경우 정확한 회계 등식(매출총이익=매출액-매출원가)으로 파생 계산 — 이는 추정이 아니라 이미 확보된 두 실측치의 차감이므로 "임의 추정 금지" 원칙 위반이 아님. 파생 계산 시 `[안내]`로 명시 표시(경고와 구분).
  - ROA/ROE 분모(자산총계/자본총계 평균)는 전기 데이터가 있으면 (당기+전기)/2, 없으면(전전기의 경우) 기말값을 그대로 사용
  - `main.py`에 비율 테이블 출력, `--csv PATH` 옵션으로 CSV 저장(utf-8-sig) 추가, 면책 문구 추가
  - `tests/test_ratios.py` 신규 6건 — account_id/account_nm 폴백 매칭, 매출총이익 파생 계산, 매핑 실패 경고, ROA/ROE 평균 로직, 증감률, 분모 결측 시 공란 처리 검증
- **실데이터 검증**: 삼성전자 2025 사업보고서(위 링크)로 실행 → 매핑 실패 0건, 파생 계산 0건(매출총이익 직접 태깅됨). 유동비율/부채비율/자기자본비율을 원본 재무제표 수치와 수동 대사(reconciliation)하여 오차 없이 일치 확인 (예: 당기 유동비율 139.08% = 유동자산 101.44조 / 유동부채 72.94조)
- **결론**: Phase 2 Definition of Done(3개년 비율 표 출력 + CSV 다운로드) 충족. 테이블 UI는 현재 CLI 텍스트 출력 수준 — Streamlit 등 그래픽 UI 전환은 Phase 3(그래프 시각화) 착수 시 함께 검토 필요 (오픈 이슈로 남김)
- **미검증/오픈 이슈**: 매출총이익 미태깅 + 매출원가도 없는 회사(매출총이익 자체 표시 불가한 업종, 예: 금융업) 케이스는 실데이터로 아직 미검증. 계정과목명이 표준 alias 목록에 없는 회사(중소기업 등 비표준 명칭 사용) 케이스도 미검증

### 2026-07-17 — Phase 2/3 설계 보강: 비율 그래프 + 원 항목(절대금액) 그래프 병행
- 논의 배경: 비율만 시각화할 경우 (1) 비율이 동일해도 절대 규모가 함께 축소/확대되는 경우, (2) 자산·부채 구성비가 크게 이동하는 경우, (3) 중요성(materiality) 판단에 필요한 금액 비중을 놓칠 수 있다는 문제 제기
- 결론: 비율 그래프(메인)와 원 항목 스택 바 차트(보조/드릴다운)를 계층적으로 배치하는 방향으로 확정, Phase 2(원 금액 보존)·Phase 3(원 항목 그래프 UI)에 반영

### 2026-07-17 — 위 설계를 `src/excel_export.py`에 구현 및 실데이터 검증 완료
- 참고: Phase 3(그래프 시각화)는 README 기준 이미 상당 부분 구현되어 있었음 — 재무비율 8개 계산, ±20% 조건부 서식 강조, 재무상태표 항목 클러스터형 막대그래프까지는 기존에 완료된 상태였고, 이번에 빠져 있던 "비율 자체의 그래프(메인)"와 "구성비가 드러나는 스택형 원 항목 그래프"만 추가함
- `_add_ratio_chart` 신규: "재무비율" 시트에 유동비율/부채비율/자기자본비율/ROA/ROE 5개 핵심 비율의 3개년 라인차트를 비율표 옆에 배치 (감사 위험 판단의 메인 그래프)
- 기존 `_add_bs_item_chart`(클러스터형 4항목 막대)를 `_add_stacked_item_chart`로 교체: 자산(유동자산/비유동자산)과 부채(유동부채/비유동부채)를 각각 별도의 스택 바 차트로 분리 — 자산·부채를 하나로 합치면 자산총계도 부채총계도 아닌 의미 없는 합계가 되므로 분리함
- 화면 배치: "재무비율" 시트(비율 그래프, 메인)와 별도로 새 시트 "상세보기_원항목추이"(원 항목 스택 차트 2개, 보조/드릴다운)를 분리 — 워크북 탭 자체를 "별도 탭"으로 활용해 PRD 권고안(비율 먼저 → 이상 신호 시 상세보기로 드릴다운)을 그대로 구현
- 실데이터 검증: 덴티움 2025 사업보고서(별도재무제표, `rcpNo=20260323001196&dcmNo=11166205`)로 `main.py` 재실행 → `덴티움_별도_결과.xlsx` 재생성, 시트 4개(안내/재무제표/재무비율/상세보기_원항목추이) 및 차트 3개(라인 1 + 스택바 2) 정상 생성 확인 (`openpyxl.load_workbook`으로 시트/차트/계열명 구조 검증)
- 기존 pytest 23건 전체 통과 (excel_export.py는 아직 단위 테스트 없음 — 오픈 이슈로 남김)
- **오픈 이슈**: excel_export.py에 대한 단위 테스트(차트 계열 구성, 빈 데이터 처리 등) 아직 없음 → 추후 보강 필요

### 2026-07-17 — 버그 수정: 연결/별도재무제표 오인식
- **증상**: 사용자가 SK하이닉스 "연결" 감사보고서 링크를 입력했는데 별도재무제표로 조회됨
- **원인**: 계정과목 내용으로 연결/별도를 추론하는 로직이 있었던 게 아니라, `main.py`의 `--fs-div` CLI 옵션 기본값이 `"OFS"`로 하드코딩되어 있었고, URL의 `dcmNo`(문서 식별자)는 애초에 파싱조차 되지 않아 링크가 가리키는 문서 종류와 무관하게 항상 별도로 조회되던 것이 원인
- **수정**: `dart_client.py`에 `fetch_financial_statements_auto()` 신규 추가 — 계정 내용 추론 없이 API 응답 자체로 판별: `fs_div` 미지정 시 CFS(연결) 먼저 요청 → 응답이 비어있으면(status="013", 연결 대상 종속기업 없음) OFS(별도)로 자동 폴백. `--fs-div`를 명시하면 폴백 없이 그 값만 사용
  - 상장사 감사보고서의 주 재무제표는 K-IFRS상 연결이므로 무지정 시 CFS 우선이 감사 실무 기대치에 부합
  - `main.py`: `--fs-div` 기본값을 `None`(자동)으로 변경, 실제 조회에 쓰인 `fs_div_used`로 콘솔 안내·엑셀 파일명 결정
  - `excel_export.py` 안내 시트: "연결재무제표(CFS)" / "별도재무제표(OFS) — 연결재무제표가 공시되어 있지 않아 자동 전환됨"으로 명확히 표시해 어떤 기준인지 헷갈리지 않도록 함
- **검증**: `tests/test_dart_client.py` 신규 3건(CFS 우선 사용/CFS 빈 응답 시 OFS 폴백/명시적 override 시 폴백 없음) 추가, 전체 pytest 26건 통과. SK하이닉스 링크를 옵션 없이 재실행해 `SK하이닉스_연결_결과.xlsx`(유동비율 185.82% 등, 이전 별도 결과 134.05%와 확연히 다름)로 정상 전환됨을 실측 확인. 덴티움도 CFS가 존재하는 회사로 확인됨(재실행 시 연결로 조회됨)
- **미검증**: CFS가 실제로 없는 회사(연결 대상 종속기업 없음)로 OFS 자동 폴백 경로는 아직 실데이터로 검증 못함(단위 테스트로만 검증) — 그런 회사 링크를 확보하면 추가 검증 필요

### 2026-07-17 — 링크의 dcmNo로 연결/별도 의도 판별 (CFS 둘 다 있는 회사 대응)
- **증상**: 연결·별도 재무제표가 둘 다 공시된 회사(인바이츠바이오코아)에서, 사용자가 별도 감사보고서 문서 링크(dcmNo가 "감사보고서"를 가리킴)를 줬는데도 CFS 우선 자동판별 로직 때문에 항상 연결로 조회됨. "CFS가 있으면 무조건 CFS"라는 이전 수정(2026-07-17 앞선 항목)의 설계 한계
- **조사**: 링크의 dcmNo를 어떻게든 활용할 방법을 재검토. 공식 Open API(`document.xml`)에는 dcmNo→문서종류 매핑이 없지만, 사용자가 실제로 붙여넣는 그 URL 자체(`dart.fss.or.kr/dsaf001/main.do?rcpNo=...&dcmNo=...`)가 공개 웹페이지이고, 그 안에 `makeToc()` 자바스크립트로 문서 목차(TOC)가 그대로 내려온다는 걸 확인 — 여기서 dcmNo가 가리키는 문서명을 읽을 수 있음
- **함정과 재수정**: 처음엔 TOC 최상위 노드(`node1`) 제목만 읽었는데, 실측해보니 DART는 연결/별도 감사보고서 모두 최상위 제목을 "감사보고서"로 동일하게 표시하고("독립된 감사인의 감사보고서" 등도 마찬가지), 실제 구분은 하위 항목("(첨부)연결재무제표" vs "(첨부)재무제표")에서만 드러남을 발견. SK하이닉스 링크로 회귀 테스트하다가 최상위 노드만 보는 방식이 SK하이닉스의 실제 연결 링크를 별도로 오판하는 걸 발견해 즉시 재수정 — 목차 전체 텍스트에서 "연결" 포함 여부로 판별하도록 변경
- **구현**:
  - `src/link_parser.py`: `extract_dcm_no()` 신규 — URL에서 dcmNo 추출(없어도 에러 아님, 힌트일 뿐)
  - `src/doc_tree.py` 신규: `guess_fs_div(rcept_no, dcm_no)` — DART 뷰어 페이지의 TOC 텍스트 전체에서 "연결" 포함 여부로 CFS/OFS 추정. 공식 API가 아니라 공개 페이지를 읽는 방식이라 실패해도 예외 없이 `(None, None)` 반환 → 호출부가 기존 CFS 우선 자동판별로 안전하게 폴백
  - `src/dart_client.py`: `fetch_financial_statements_auto()`에 `fs_div_hint` 매개변수 추가. 우선순위: `--fs-div`(사용자 명시) > 링크 dcmNo 힌트 > CFS 우선 기본값. 힌트가 가리키는 기준의 데이터가 실제로 없으면 반대쪽으로 폴백
  - `main.py`: dcmNo 힌트 사용 여부와 근거를 콘솔에 안내 출력, 엑셀 안내 시트에도 판단 근거(`fs_note`) 표시
  - `tests/test_link_parser.py`, `tests/test_doc_tree.py`(신규), `tests/test_dart_client.py`에 총 9건 테스트 추가 — 전체 35건 통과
- **실측 검증**: 인바이츠바이오코아 별도 링크(`dcmNo=11158217`) → 별도로 정확히 인식(유동비율 68.45% 등, 이전 CFS 오판 시 16.04%와 다름). SK하이닉스 연결 링크(`dcmNo=11132575`, 최초 버그 리포트 링크) → 연결로 정확히 인식. 정관/영업보고서 등 무관한 dcmNo는 판별 불가(None)로 안전하게 처리되어 CFS 우선 기본값으로 폴백함을 확인
- **한계**: 이 판별은 DART 공식 API가 아니라 공개 뷰어 페이지의 HTML/JS 구조를 읽는 방식이라, DART가 페이지 구조를 바꾸면 조용히 동작을 멈출 수 있음(예외 없이 None 반환 → 자동으로 기존 CFS 우선 로직으로 폴백하므로 크래시나 잘못된 값 반환은 없지만, 힌트 기능 자체는 무력화될 수 있음). 주기적으로 실제 링크로 재검증 필요

### 2026-07-17 — 독립 검증(재계산 대사 + 재무상태표 등식)을 엑셀 생성 파이프라인에 내장
- **배경**: 지금까지는 회계사가 특정 회사 결과를 의심할 때마다("부채비율 변동이 비경상적인데요?") 매번 임시 스크립트로 수동 대사(비율을 별도 코드로 재계산해 비교, 자산=부채+자본 등식 확인)를 돌려왔음. 사용자가 이 절차를 산출물 자체에 자동으로 포함시켜 달라고 요청
- **구현**: `src/verification.py` 신규
  - `verify_ratios()` — 유동비율/부채비율/자기자본비율을 `ratios.py`의 `compute_ratios`와 **완전히 독립된 코드**로 재계산해 대사(같은 함수를 두 번 호출하면 그 함수 자체의 버그를 못 잡으므로 일부러 로직을 중복 작성). ROA/ROE/수익성 비율은 평균 분모 로직 자체가 정의의 일부라 독립 재계산의 의미가 약해 제외
  - `verify_balance_sheet_identity()` — 자산총계=부채총계+자본총계, 자산총계=유동자산+비유동자산 항등식이 실제로 성립하는지 확인(표준화/파생계산 로직 자체의 정합성 검증)
  - 계정 매핑 실패로 값이 결측인 경우는 검증 대상에서 제외(이미 별도 경고로 처리되는 케이스라 중복 경고 방지)
  - `src/excel_export.py`에 "검증" 시트 신규 추가 — 대사 결과 표 + 등식 검증 표, 불일치 항목은 빨간색(WARN_FILL)으로 강조, 상단에 "✓ 전체 N건 모두 일치" 또는 "⚠ 불일치 N건 발견" 요약
  - `main.py`에서도 실행할 때마다 콘솔에 검증 결과 요약 출력, 불일치 시 경고와 함께 상세 내역 표시
  - `tests/test_verification.py` 신규 6건 — 정상 일치 케이스뿐 아니라 **의도적으로 틀린 값을 주입해 검증이 실제로 잡아내는지**까지 확인(그냥 통과만 하는 가짜 테스트 방지). 전체 pytest 41건 통과
- **실측 검증**: 인바이츠바이오코아 별도 결과(부채비율이 207%→2,255%로 급변동한 케이스)로 재실행 → 15건(비율 9건+등식 6건) 모두 "일치"로 자동 확인됨. SK하이닉스·덴티움 샘플도 재생성해 검증 시트 정상 포함 확인
- **한계**: 이 검증은 "우리 계산 로직이 스스로와 일관되는지 + 원본 데이터의 회계 항등식이 성립하는지"를 확인하는 것이지, DART 원본 공시 수치 자체가 맞는지까지 보장하지는 않음(그건 이 도구의 검증 범위 밖 — 최종 판단은 여전히 회계사 몫)

### 2026-07-17 — account_id 중복 매칭 방어 로직 추가 + 그 과정에서 발견한 심각한 버그 수정
- **요청 배경**: `_match_amount()`가 동일 account_id 매칭 행이 여러 개일 때 `.iloc[0]`로 첫 값만 쓰는 게 위험하다는 지적 — 값이 다르면 임의로 첫 값을 택하지 말고 공란+경고 처리하도록 요청받음
- **1차 구현**: `_match_amount()`가 `(금액, ambiguous여부)`를 반환하도록 변경, 값이 다르면 NaN+경고. `standardize_accounts`/`compute_ratios`가 4-tuple로 `ambiguous_matches` 전파, `main.py`에 경고 출력 추가
- **1차 구현 직후 발견한 심각한 회귀**: SK하이닉스로 재검증하다가 **자본총계·당기순이익이 전부 공란 처리되고 부채비율/자기자본비율/ROA/ROE/순이익률이 전부 NaN**이 되는 걸 발견. 원인: DART 응답은 재무상태표/손익계산서/현금흐름표/**자본변동표**를 구분 없이 한 테이블에 섞어서 주는데, 자본변동표는 구조상 "자본총계"(`ifrs-full_Equity`)나 "당기순이익"(`ifrs-full_ProfitLoss`) 같은 account_id를 기초자본/배당/당기순이익반영/기타포괄손익/기말자본 등 한 기간에 8~9개 행에서 재사용한다 — 이는 정상적인 구조이지 오류가 아닌데, `sj_nm`(재무제표 구분)을 안 보고 매칭하다 보니 전부 "충돌"로 오판함
- **근본 수정**: `ACCOUNT_ALIASES`의 각 표준계정에 `sj_nm`(유효 재무제표 구분: 재무상태표 계정은 `{재무상태표}`, 손익계산서 계정은 `{손익계산서, 포괄손익계산서}`) 추가, `_match_amount()`가 매칭 전에 이걸로 대상을 먼저 좁히도록 수정. 그 위에 값 충돌 검사(원래 요청사항)를 얹어, 진짜 같은 재무제표 내에서 같은 계정코드인데 값이 다른 경우만 공란+경고 처리하도록 함
- **검증**: SK하이닉스/덴티움/인바이츠바이오코아 재실행 → 모두 원래 정확한 비율로 복원, ambiguous 경고 0건, 검증 시트 15건 모두 일치 재확인. `tests/test_ratios.py`에 자본변동표 재사용 패턴을 실제로 재현하는 회귀 테스트 추가(재무상태표 값이 자본변동표의 다른 값에 오염되지 않는지 확인)
- **진단 스크립트**: `scripts/check_duplicate_accounts.py` 신규 — 여러 corp_code를 순회하며 `(period, sj_nm, account_id)`별 중복/충돌 여부를 표로 출력. 실측 결과 SK하이닉스/덴티움/인바이츠바이오코아/삼성전자(CFS+OFS) 전부에서 재무상태표·손익계산서 범위 내 충돌은 0건, 충돌은 전부 자본변동표(구조적으로 정상)에만 존재함을 확인. DART가 표준코드 없는 계정에 공통으로 넣는 플레이스홀더 문자열("-표준계정코드 미사용-")은 진단 대상에서 제외 처리(진짜 계정코드가 아니라 노이즈였음)
- **교훈**: 방어 로직을 추가할 때 실제 데이터로 즉시 회귀 검증하지 않았다면 이 버그를 놓친 채 배포했을 뻔함 — 앞으로 계정 매칭 로직을 건드릴 때는 반드시 이미 검증된 실제 회사(SK하이닉스 등)로 재실행해 회귀를 확인할 것

### 2026-07-17 — main.py의 RuntimeError 예외처리 좁히기
- **문제**: `API_ERRORS`에 bare `RuntimeError`가 포함되어 있어, `config.require_api_key()`의 API 키 미설정 오류뿐 아니라 이 코드와 무관한 예상치 못한 런타임 오류까지 전부 "API 호출 문제"로 오분류되어 삼켜질 위험이 있었음
- **수정**: `src/config.py`에 `class ConfigError(RuntimeError)` 신규(기존 `DartApiError`/`ResolveError`와 동일한 프로젝트 관례), `require_api_key()`가 이제 `ConfigError`를 던짐. `main.py`의 `API_ERRORS`에서 `RuntimeError` → `config.ConfigError`로 교체
- **효과**: 진짜 설정 오류(API 키 미설정)만 사용자 친화적 메시지로 잡히고, 그 외 예상치 못한 런타임 오류는 스택트레이스가 그대로 노출되어 디버깅이 쉬워짐
- **검증**: `tests/test_config.py` 신규 3건(ConfigError 발생 확인, 타입 관계 확인, 정상 케이스) — 전체 pytest 48건 통과

### 2026-07-17 — GitHub 저장소(fs-analyze)에 반영
- 로컬 작업 폴더가 git 저장소가 아니었음(원격 저장소는 이미 존재, 초기 커밋만 있는 상태) → GitHub MCP(`push_files`)로 텍스트/코드 파일 전체를 반영
- `.xlsx` 샘플 결과 파일(바이너리)은 MCP push 시 손상 위험이 있어 제외 — 별도 방법(로컬 git CLI 등)으로 갱신 필요(오픈 이슈)

### Phase 4 오픈 이슈 (사용자와 논의됨)
- DART API 키 외에 Phase 4(PER/PBR)에서 주가 데이터용 외부 API가 추가로 필요할 가능성 높음 (예: 공공데이터포털 KRX 시세정보 API, 별도 키 발급 필요)
- 발행주식수는 DART의 `stockTotqySttus.json`으로 확보 가능하여 별도 소스 불필요
